name: passwd_tray_service
description: Tray service for passwd browser based auto-fill
environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  logging: ^0.11.4
  logging_appenders: ^0.4.3

dev_dependencies:
  pedantic: ^1.9.0
